#include <stdio.h>
#include <math.h>

//pi*a^2
int main(){
const double pi=3.14;
int a,c;

printf("cemberin yaricapini giriniz:");
scanf("%d", &a);
c=pi*(a*a);
printf("cemberin alani: %d",c);

   return 0;
}